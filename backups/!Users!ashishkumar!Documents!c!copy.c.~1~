#include <cs50.h>
#include <stdio.h>
#include <ctype.h>

int main(void){
  string user = get_string("Enter user: ");
  
  string t = user;
  t[0] = toupper(t[0]);

  printf("%s", user);
  printf("%s", t);
  
}
